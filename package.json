{
  "name": "landing",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --outfile=dist/main.js --target=es2020",
    "dev": "npm run build && npx serve .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "MILINSKY",
  "license": "ISC",
  "description": "MILINSKY landing page",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.6",
    "esbuild": "^0.28.0",
    "happy-dom": "^20.9.0",
    "vitest": "^4.1.6"
  }
}
